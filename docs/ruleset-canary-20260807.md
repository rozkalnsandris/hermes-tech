# Ruleset canary — 2026-08-07

Temporary GitHub branch-protection canary.

Purpose: verify that the public repository's `main` rulesets require pull requests and the GitHub Actions `validate` check before merge.

This file is test evidence only and must not be merged into `main`.
