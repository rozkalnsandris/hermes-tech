# Ruleset canary — step 2

Second temporary canary commit used to verify that a new PR head SHA becomes non-mergeable until GitHub Actions `validate` succeeds again.

This file is test evidence only and must not be merged into `main`.
